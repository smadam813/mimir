@using Mimir.Server.Ui

@* One Wisdom in the §8.1 browser's list: kind and state badges, the text linking to its detail,
   and the inline curation affordance. The browser's rows are its only caller — the injection log
   used to drop it in too, and #96 took it back out: on the ported §8.3 surface a carried line is
   one row of a score table, and a card with its own curation buttons inside that table would
   invite retiring a Wisdom from a screen about how it ranked. *@
<article class="wisdom-card">
    <header class="wisdom-head">
        <KindBadge Kind="Entry.Kind" />
        <span class="wisdom-scope" title="Scope (§3)">@Entry.ScopeName</span>
        <StateBadges Entry="Entry" />
        <span class="wisdom-meta">
            ×@Entry.Reinforcement · @EpisodeDisplay.Stamp(Entry.LastConfirmedAt)
        </span>
    </header>
    <a class="wisdom-text" href="@($"projects/{Entry.ScopeProjectId}/wisdom/{Entry.Id}")">@Entry.Text</a>
    <WisdomCuration Entry="Entry" OnChanged="OnChanged" />
</article>

@code {
    [Parameter, EditorRequired]
    public required WisdomListEntry Entry { get; set; }

    [Parameter]
    public EventCallback OnChanged { get; set; }
}
